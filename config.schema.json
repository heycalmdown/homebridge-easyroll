{
  "pluginAlias": "easyroll",
  "pluginType": "accessory",
  "singular": false,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "Easyroll Blind",
        "description": "Name of accessory, as displayed in HomeKit."
      },
      "ip": {
        "type": "array",
        "title": "ip",
        "required": true,
        "description": "IPs of a group of an Easyroll blind",
        "items": {
          "title": "IP",
          "type": "string"
        }
      },
      "flip": {
        "type": "boolean",
        "title": "flip",
        "required": false,
        "description": "Flip the direction"
      }
    }
  }
}