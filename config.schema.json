{
  "pluginAlias": "easyroll",
  "pluginType": "accessory",
  "singular": false,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "Easyroll Blind",
        "description": "Name of accessory, as displayed in HomeKit."
      },
      "ip": {
        "type": "string",
        "title": "ip",
        "required": true,
        "description": "IP of an Easyroll blind"
      }
    }
  }
}